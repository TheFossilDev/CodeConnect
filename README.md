# CodeConnect ReactJS Frontend

## Steps to build:
1. Make sure Node and NPM 
2. Run `npm install` to install all packages required for running
3. To run a local dev server, run `npm start`
4. To build for deployment, run `npm build`